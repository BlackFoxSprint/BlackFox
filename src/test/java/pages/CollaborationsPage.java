package pages;

import elements.CollaborationsElements;
import org.openqa.selenium.WebDriver;

public class CollaborationsPage extends CollaborationsElements {
    public CollaborationsPage(WebDriver driver) {
        super(driver);
    }
}
